const config = {
  plugins: {
    // Tailwind v4 PostCSS plugin — vendor prefixes are handled automatically,
    // so autoprefixer is no longer needed as a separate plugin.
    '@tailwindcss/postcss': {},
  },
};

export default config;
